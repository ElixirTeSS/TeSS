<div class="biotools-info">
    {{#if labels}}
        <div class="tooltypes">
          {{#each labels}}
            <span class="label label-info">{{ this }}</span>
          {{/each}}
        </div>
    {{/if}}
    <span>{{{ truncatedDescription }}}</span>
</div>
