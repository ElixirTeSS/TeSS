<li data-id="{{ item.uri }}">
    <input type="hidden" name="{{ prefix }}[]" value="{{ item.uri }}" />
    {{ item.preferred_label }} <span class="muted">{{ item.uri }}</span>
    <a href="#" class="delete-list-item btn btn-icon">
        <i class="icon cross-icon icon-sm icon-greyscale"></i>
    </a>
</li>
