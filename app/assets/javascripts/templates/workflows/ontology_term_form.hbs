<li class="ontology-term" data-attribute-id="{{ id }}" data-attribute-label="{{ label }}" data-attribute-url="{{ url }}">
    {{ label }} <span class="muted">{{ url }}</span>
    <a href="#" class="delete-list-item">&times;</a>
</li>
