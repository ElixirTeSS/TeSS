<% if !objects.blank? && objects.count > 0 %>
  <ul class="masonry media-grid">
    <% objects.each do |object| %>
      <% cache_unless(current_user&.is_admin?, [controller_name, object.cache_key]) do %>
        <%= render object %>
      <% end %>
    <% end %>
  </ul>
<% end %>

