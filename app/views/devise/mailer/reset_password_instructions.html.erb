<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<p><%= t("devise.mailer.confirmations.password_change_request")%></p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("devise.mailer.confirmations.ignore_if_not_you")%></p>
<p><%= t("devise.mailer.confirmations.password_change_waits")%></p>
