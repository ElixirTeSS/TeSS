<% if !activity.parameters.empty? %>
    <i class="fa fa-pencil-square-o"></i>

    <% if activity.owner %>
        <%= link_to activity.owner.username, activity.owner %>
    <% end %>

    added to package
    <%= link_to activity.parameters[:package_title], package_path(activity.parameters[:package_id]) %>
    at
    <%= activity.created_at -%>.
<% end %>
