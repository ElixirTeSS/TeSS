<h1>Exciting Happenings</h1>

<% @activities.each do |activity| %>
    <div class="activity">
        <% if activity.owner -%>
            <%= link_to activity.owner[:username], activity.owner  %>
        <% else -%>
            Someone
        <% end -%>
        <%= render_activity activity %>
    </div>
<% end %>
