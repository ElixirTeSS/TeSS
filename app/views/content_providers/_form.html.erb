<div class="row">
  <div class="col-md-9 col-xs-9">
    <%= simple_form_for @content_provider, :html => { :class => "form-horizontal content_provider" } do |f| %>

        <% if @content_provider.errors.any? %>
            <div id="error_expl" class="panel panel-danger">
              <div class="panel-heading">
                <h3 class="panel-title"><%= pluralize(@content_provider.errors.count, "error") %> prohibited this content_provider from being saved:</h3>
              </div>
              <div class="panel-body">
                <ul>
                  <% @content_provider.errors.full_messages.each do |msg| %>
                      <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            </div>
        <% end %>

        <div class="form-group">
          <%= f.label :title %>
          <div class="controls">
            <%= f.text_field :title, :class => 'form-control' %>
          </div>
          <%#= error_span(@content_provider[:title]) %>
        </div>
        <div class="form-group">
          <%= f.label :url, "URL" %>
          <div class="controls">
            <%= f.text_field :url, :class => 'form-control' %>
          </div>
          <%#= error_span(@content_provider[:url]) %>
        </div>
        <div class="form-group">
          <%= f.label :logo_url, "Logo URL" %>
          <div class="controls">
            <%= f.text_field :logo_url, :class => 'form-control' %>
          </div>
          <%#= error_span(@content_provider[:logo_url]) %>
        </div>
        <div class="form-group">
          <%= f.label :description %>
          <div class="controls">
            <%= f.text_area :description, :class => 'form-control' %>
          </div>
          <%#= error_span(@content_provider[:description]) %>
        </div>

        <%= render :partial => 'common/multiple_input', :locals => {:field_name => 'keywords', :resource => @content_provider, :f => f} %>

        <div class="form-group">
          <%= f.submit ( f.object.new_record? ? "Register" : "Update") + " content provider", :class => 'btn btn-primary' %>
          <%= link_to 'Back', content_providers_path, :class=>"btn btn-info" %>
          <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                      content_provider_path, :class => 'btn btn-default' %>
        </div>
    <% end %>
  </div>
</div>