<div class="listing_item">
  <div class="h3"><%= link_to(content_provider.title, content_provider) %></div>
  <p><%= content_provider.url %></p>
  <p><%= content_provider.description %></p>
    <%= image_tag content_provider.logo_url, :class=>"listing_image" %>
</div>
