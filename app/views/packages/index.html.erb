<!-- SIDEBAR | RESULTS -->
<div class="row">
  <!-- SIDEBAR -->
  <div class="col-md-3">
    <%= render :partial=> "search/common/facet_sidebar", :locals=>{:resources=>@packages} %>
  </div>

  <!-- RESULTS -->
  <div class="col-md-9">
    <!-- SEARCH BAR -->
    <div class="row">
      <%= render :partial=> "search/common/search_box", :locals=>{:resource_path => packages_path} %>
    </div>
    
    <!-- APPLIED FILTERS AND NUMBER OF RESULTS -->
    <div class="row">
      <%= render :partial => "search/common/search_info", :locals=>{:resource=>@packages}  %>
    </div>

    <%#= render :partial=> "search/common/pagination_bar", :locals=>{:resources=>@materials} %>
    <hr>

    <!-- ACTUAL RESULTS LIST -->
    <div class="row">
      <% @packages.results.each do |package| %>
         <%= render package %>
      <% end %>    
    </div>
  </div>
</div>
