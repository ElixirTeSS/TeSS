<%- model_class = User -%>
<div class="page-header">
  <h2><%=t '.title', default: 'Users' %></h2>
</div>

<ul class="masonry media-grid" style="margin-top: 15px;">
  <% @users.each do |user| %>
      <%= render user unless user.role == Role.fetch('default_user')%>
  <% end %>
</ul>
