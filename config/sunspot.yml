production:
  solr:
    #hostname: localhost
    #port: 8983
    #log_level: WARNING
    #path: /solr/production
    # read_timeout: 2
    # open_timeout: 0.5
    #url: http://<%= ENV['SOLR_USERNAME'] %>:<%= ENV['SOLR_PASSWORD'] %>@localhost
    host: localhost
    port: 8080
    log_level: WARNING
    path: /solr

development:
  solr:
    hostname: localhost
    port: 8982
    log_level: INFO
    path: /solr/development

test:
  solr:
    url: http://<%= ENV['SOLR_USERNAME'] %>:<%= ENV['SOLR_PASSWORD'] %>@tess.oerc.ox.ac.uk
    port: 8080
    log_level: WARNING
    path: /solr
